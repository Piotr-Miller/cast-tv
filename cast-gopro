#!/usr/bin/env python3
"""Thin shim: the code lives in castlib; this file keeps the symlink install working."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
from castlib.cli import main_gopro  # noqa: E402

sys.exit(main_gopro())
